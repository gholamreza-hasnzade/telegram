// * import config
import { AppConfigurations } from "@configs/app-configurations/app.configurations";

export const App = () => {
    return (
        <>
            <AppConfigurations>
                <h1>Reza</h1>
            </AppConfigurations>
        </>
    );
};
