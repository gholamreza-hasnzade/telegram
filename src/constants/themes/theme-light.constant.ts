import { ITheme } from "@constants/themes/theme.constant.interface"

const BASE_COLORS = {
    WHITE: '#ffffff',
    BLACK: '#000000'
};








export const THEME_LIGHT: ITheme = {
    ...BASE_COLORS,
};